apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer-deploy
  namespace: stock
  labels:
    app: producer-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-producer
  template:
    metadata:
      labels:
        app: stock-producer
    spec:
      containers:
        - name: stock-producer
          image: xxxxxx.dkr.ecr.ap-northeast-2.amazonaws.com/stock/xxxx:latest
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          envFrom:
          - configMapRef:
              name: producer-config-dev
          - secretRef:
              name: producer-secret-dev

      imagePullSecrets:
        - name: stock-producer

      nodeSelector:
        key: kube-master1
